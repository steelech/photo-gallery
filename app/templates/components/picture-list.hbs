{{#each pictures as |row|}}
	<div class="row">
		{{#each row as |picture|}}
			<div class="col-md-4">
				<div class="thumbnail">
					<a {{action "openSlideshow" picture}}>
						<img src={{picture.url}} style="width: 100%">
					</a>
				</div>
			</div>
		{{/each}}
	</div>
{{/each}}
