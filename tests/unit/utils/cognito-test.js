import cognito from 'photo-gallery/utils/cognito';
import { module, test } from 'qunit';

module('Unit | Utility | cognito');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cognito();
  assert.ok(result);
});
